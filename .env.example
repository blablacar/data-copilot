# Environment variables for BigQuery configuration
PROJECT_ID=Your-Project-ID-Here

# BigQuery region for query execution (e.g., US, EU)
BQ_REGION=EU

# Regex pattern to filter datasets (e.g., "^(staging|prod)_.*" to match datasets starting with staging_ or prod_)
# Leave empty or comment out to process all datasets
DATASET_FILTER_REGEX="^(samples|covid19_google_mobility|covid19_geotab_mobility_impact)$"

# Regex pattern to filter tables (e.g., "^fact_.*" to match tables starting with fact_)
# Leave empty or comment out to process all tables
TABLE_FILTER_REGEX=""
